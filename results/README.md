# Results

`latest_run_summary.json` is the canonical summary for the updated run and is
the source for the metrics in the root README and resume.

Files under `legacy/` are retained from earlier runs. They used different
checkpoints or prompt settings and must not be compared with the updated run or
used as the current headline results.
